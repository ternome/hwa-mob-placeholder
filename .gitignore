# OS
.DS_Store

# Local preview helpers (sandbox/iCloud workaround — not needed for deploy)
.claude/
serve.py

# Generated by build.py — source of truth is src/index.template.html + locales/.
# Vercel runs `python3 build.py` (buildCommand) on every deploy to regenerate.
# Locally: run `python3 build.py` after editing the template or any locale.
# Root-anchored so they don't match src/index.template.html.
/index.html
/index.*.html
/sitemap.xml
/robots.txt
